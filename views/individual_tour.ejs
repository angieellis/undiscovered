<div class="not-index container">

<div id="individual-tour-display">

</div>


<script id="individual-tour-template" type="text/x-handlebars-template">

  <div class="tour-text p-sm debug">
    <div class="fz-lg debug">
      {{title}}
    </div>

    <div class="location fz-sm debug">
      {{city}}, {{state}}
    </div>

    <div class="description fz-xs debug">
      {{trailer.description}} Im jstoijaf;ajsdf;lkja alkdjf;klsdjfadf asjf adding text to have a bigger description and what not a;kdfj;askdfj.skjljse ;fkajdsflkjas;dfkj.kzmklj;fkasf.kda flskfj.askdjf; jfaksldjf a;lkdjfa;sdlkfjals;dkfj jf;lkasjdf ;akldjsf;alskdjf fjsiekd fjfisle  jfifl jsi jfieleke tjtke eke ghs srtkaofdj lkjf wjek jflfjskljf
    </div>

    <div class="debug">
      {{tags}}
    </div>

    <p id="coordinates">{{coordinates}}</p>
  </div>

  <div class="actions span-4-t debug">
    <i class="fa fa-star-o fa-4x"></i>
    Option to Favorite or add to Wishlist
  </div>

  <div class="user-info span-4-t debug">
    {{tour_guide.username}}
  </div>

    <img src="{{trailer.photo_url}}" class="trailer">
    <iframe width="420" height="345"
      src="http://www.youtube.com/embed/{{video_id}}">
    </iframe>
</script>

<script src="https://maps.googleapis.com/maps/api/js?v=3.exp&signed_in=true"></script>

<script>

var coordinate = $('#coordinates').html().split(",");
// var coordinate = [71.0589, 42.3601]
function initialize() {
  var mapOptions = {
    zoom: 10,
    center: new google.maps.LatLng(coordinate[1], coordinate[0]
)
  }
  var map = new google.maps.Map(document.getElementById('map-canvas'),
                                mapOptions);
  setMarkers(map, coordinate);
}

function setMarkers(map, locations) {
  console.log(coordinate)
  console.log("Google map is working")
  var shape = {
      coords: [1, 1, 1, 20, 18, 20, 18 , 1],
      type: 'poly'
  };
  var myLatLng = new google.maps.LatLng(coordinate[1], coordinate[0]
);
  var marker = new google.maps.Marker({
      position: myLatLng,
      map: map,
      shape: shape
  });
}

google.maps.event.addDomListener(window, 'load', initialize);

</script>
<div  class="debug" id="map-canvas"></div>

</div>